---

ansible_ssh_common_args: '-o StrictHostKeyChecking=accept-new'

# common
common_apt_pkgs:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg
  - ncdu
  - vim-nox

user_bash_file: .bash_aliases
